<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Joshua's Corner</title>
    <!-- Add Tailwind CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet" />
</head>

<body class="bg-gray-100 text-gray-900 font-sans">

    <main class="container mx-auto p-6">
        <!-- Header Section -->
        <div id="header" class="text-center mb-10">
            <!-- Title -->
            <h1 id="title" class="text-4xl font-bold text-indigo-600">
                Joshua's Corner
            </h1>

            <!-- Navbar -->
            <ul id="navbar" class="flex justify-center mt-6 space-x-6">
                <li>
                    <a href="/" class="text-gray-700 hover:text-indigo-600 text-lg">Home</a>
                </li>
                <li>
                    <a href="/dash" class="text-gray-700 hover:text-indigo-600 text-lg">Dashboard</a>
                </li>

                <!-- if the user has logged in -->
                {{#if logged_in}}
                <li>
                    <!-- Logout Button -->
                    <button id="logout-button"
                        class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600 transition duration-300">
                        Log Out
                    </button>
                </li>
                {{else}}
                <li>
                    <!-- Login Button -->
                    <button id="login-button"
                        class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 transition duration-300">
                        Log In
                    </button>
                </li>
                {{/if}}
            </ul>
        </div>

        <!-- Content Section -->
        <div id="content" class="bg-white shadow-md rounded-lg p-6">
            <!-- Body of the page for content -->
            {{{body}}}
        </div>

        <!-- Error Message -->
        {{#if error}}
        <p class="error text-red-500 text-center mt-4">{{error}}</p>
        {{/if}}
    </main>

    <script src="/js/main.js"></script>
</body>

</html>
